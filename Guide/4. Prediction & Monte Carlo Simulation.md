✦ Once we have a trained model that can predict LLPS propensity for a given protein, the next step is to simulate that behavior under physiological conditions:
✧ Integrate Monte-Carlo simulations as described in the IPPcode repository) to simulate phase separation & guide physiological interaction

### ✦ PLAN:
1. Input the predicted LLPS propensity from the model into into the Monte Carlo (IPP) simulation code
2. The IPPcode then uses this propensity to predict phase separation behavior based on the parameters of the simulation.
3. Run the Monte Carlo simulations using the output from your AI model (e.g., likelihood of phase separation) as a parameter in the IPPcode framework

