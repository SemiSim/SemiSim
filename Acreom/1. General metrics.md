**✧ Model Evaluation**: After training, we'll evaluate the model using a separate test set (which the model hasn't seen before) to assess performance (Ry will prep this using code in ). 

semisim_eval_v1.py
```python
test_loss, test_acc = model.evaluate([X_test_sequences, X_test_additional_features], Y_test_labels)
print(f'Test accuracy: {test_acc}')

```

**✧ Hyperparameter Tuning**: Experiment with different architectures (e.g., LSTM vs. GRU), the number of units, dropout rates, learning rates, etc., to fine-tune performance.