
Using the pre-processed data from ry :

semisim_training_v1.py
```python
#For FASTA data ready as X_sequences (input sequences) and Y_labels (LLPS labels)
# If you have additional features, you can pass them as a list: [X_sequences, X_additional_features]

model.fit([X_sequences, X_additional_features], Y_labels, batch_size=32, epochs=20, validation_split=0.2)

```
 

















































































































